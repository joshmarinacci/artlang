@board("pygamer")

font = terminalio.FONT
color = BLUE
text = "PYGAMER"

text_area = label.Label(font:font, text:text, color:color)
display.show(text_area)
text_area.x = 60
text_area.y = 60
time.sleep(5)

@type('loop',myloop)
fun myloop() {
    pass
}
